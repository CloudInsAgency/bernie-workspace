
# Free tier quota status

As of 2026-05-03, Google's free tier returns 0 daily quota for image generation API.
Skill is installed but inactive until billing is enabled OR free tier is restored.
Cost if billing enabled: ~$0.04 per flash2 image, ~$0.07 per nb2, ~$0.13 per pro.
Re-test by running the smoke test — if it succeeds, skill is operational.
